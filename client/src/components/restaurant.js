function Restaurant(props) {
  return <h1>Yelpington!</h1>;
}

export default Restaurant;



//function to make names capital letters
// const name = (name) => s.split("-").map((name) => (name[0].toUpperCase() + name.slice(1))).join(" ")
